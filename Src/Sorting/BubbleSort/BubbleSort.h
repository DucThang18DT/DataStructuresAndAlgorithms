#ifndef _BUBBLESORT_H_
#define _BUBBLESORT_H_

    #include "../Material.h"

    // sort array of inergers 
    void bubbleSort(int*, int, Direction);

    // sort array of floats
    void bubbleSort(float *, int, Direction);

#endif