#ifndef _MATERIAL_H_
#define _MATERIAL_H_

// Direction of sortting
    // S2L : small to large
    // L2S : large to small
enum Direction{S2L, L2S};

// swap position of 2 integers
void _swap(int*, int*);

#endif