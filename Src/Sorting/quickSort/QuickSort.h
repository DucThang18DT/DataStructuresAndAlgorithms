#ifndef _QUICKSORT_H_
#define _QUICKSORT_H_

    #include "../Material.h"

    // sort array of inergers 
    void quickSort(int*, int, int, Direction);

    // sort array of floats
    void quickSort(float *, int, int, Direction);
#endif